- name: Copy ouimeaux service file
  become: yes
  template:
    src: "{{ wemo_service_file }}.j2"
    dest: /etc/systemd/system/{{ wemo_service_file }}
    mode: 0755

- name: Start ouimeaux service
  become: yes
  systemd:
    state: started
    enabled: yes
    daemon_reload: yes
    name: wemo
